name: Daily AI News

on:
  workflow_dispatch:
  # schedule:
  #   # 09:00 Asia/Seoul, every day
  #   - cron: "0 0 * * *"

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled workflow disabled
        run: echo "Scheduled daily news workflow is disabled."
